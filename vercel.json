{
    "version": 2,
    "installCommand": "yarn install",
    "buildCommand": "turbo run build --filter=auth...",
    "outputDirectory": "apps/microservices/auth/dist",
    "builds": [
        {
            "src": "apps/microservices/auth/dist/main.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "apps/microservices/auth/dist/main.js"
        }
    ]
}